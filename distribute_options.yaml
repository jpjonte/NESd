output: dist/
releases:
  - name: nesd
    jobs:
      - name: nesd-release-linux-deb
        package:
          platform: linux
          target: deb
          build_args:
            release: true
      - name: nesd-release-linux-rpm
        package:
          platform: linux
          target: rpm
          build_args:
            release: true
