name: nesd_workspace
publish_to: none

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

workspace:
  - packages/nesd
  - packages/nesd_audio
  - packages/nesd_texture
