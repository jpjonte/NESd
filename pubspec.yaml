name: nes
description: "NES emulator"
publish_to: 'none'

version: 0.0.1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  build_runner: ^2.4.10
  file_picker: ^8.0.3
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.5
  flutter_riverpod: ^2.5.1
  hooks_riverpod: ^2.5.1
  mp_audio_stream:
    git:
      url: ssh://git@ssh.git.jpj.dev:8022/bud/mp-audio-stream.git
      ref: feature/buffer-size
  riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  riverpod_generator: ^2.4.0

dev_dependencies:
  dart_code_metrics: ^5.7.6
  flutter_test:
    sdk: flutter

  lint: ^2.1.2

flutter:
  uses-material-design: true
