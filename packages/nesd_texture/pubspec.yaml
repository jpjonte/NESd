name: nesd_texture
description: NESd's GPU texture plugin for presenting PPU frames.
version: 0.0.1

environment:
  sdk: '>=3.12.0 <4.0.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: dev.jpj.nesd_texture
        pluginClass: NesdTexturePlugin
      ios:
        pluginClass: NesdTexturePlugin
      linux:
        pluginClass: NesdTexturePlugin
      macos:
        pluginClass: NesdTexturePlugin
      windows:
        pluginClass: NesdTexturePluginCApi
